# Corrective Retrieval-Augmented Generation (C-RAG) Capstone Project

This project implements a Corrective Retrieval-Augmented Generation (C-RAG) tutoring system designed to answer questions using a local corpus or fall back to web search when the retrieved local documents are insufficient. The backend is powered by FastAPI and LangGraph, utilizing local models via Ollama and ChromaDB for vector storage. The frontend is built with Next.js, featuring a clean, light-themed monochromatic dashboard styled with Vanilla CSS.

---

## System Overview and Architecture

The C-RAG system operates as a state-graph workflow that dynamically evaluates the quality of retrieved documents before generation:

1. **Retrieve**: Queries a local ChromaDB instance using embeddings generated by Ollama (`embeddinggemma:latest`) to fetch the top 4 relevant document chunks from the reference PDF textbooks (`book1.pdf`, `book2.pdf`, `book3.pdf`).
2. **Evaluate**: A local LLM judge (`gemma3:4b`) scores each retrieved chunk. 
   - A score above 0.7 marks a document as strongly relevant.
   - A score below 0.3 marks a document as irrelevant.
   - The overall verdict is classified as:
     - **CORRECT**: If at least one chunk scores above 0.7. The system proceeds directly to knowledge refinement.
     - **INCORRECT**: If all chunks score below 0.3. The system ignores local documents and rewrites the query for web search.
     - **AMBIGUOUS**: If chunks fall between thresholds. The system retains weakly relevant local documents and supplements them with web search.
3. **Web Search Fallback**: If the verdict is `INCORRECT` or `AMBIGUOUS`, the query is rewritten into search keywords and sent to the Tavily Search API. 
4. **Knowledge Refinement (Sentence-Level Filtering)**: All selected documents (local, web, or mixed) are merged and decomposed into individual sentences. A binary LLM relevance judge evaluates each sentence, keeping only those that directly contribute to answering the query and dropping the rest to minimize context noise.
5. **Generation**: The refined context is passed to the local LLM (`gemma3:4b`) to generate a tutor response. If the context is empty, the model outputs a standard fallback answer.

---

## Technical Stack

* **Backend**: FastAPI, LangGraph (graph orchestration), LangChain (framework), ChromaDB (vector database), Ollama (local LLM engine).
* **Frontend**: Next.js (App Router, React, TypeScript), Vanilla CSS (monochromatic light theme), Lucide React (icons).
* **AI Models**:
  - LLM: `gemma3:4b` (Local via Ollama)
  - Embeddings: `embeddinggemma:latest` (Local via Ollama)

---

## Project Structure

```
/Volumes/SSD/C-RAG/
├── Docs/                    # Source PDF documents folder
│   ├── book1.pdf
│   ├── book2.pdf
│   └── book3.pdf
├── backend/
│   ├── chroma_db/           # Symlink to local APFS storage (Chroma database)
│   ├── main.py              # FastAPI endpoints and route handlers
│   ├── rag_agent.py         # LangGraph state machine & node operations
│   ├── requirements.txt     # Python backend dependencies
│   ├── .env                 # Environment configuration (Tavily API key)
│ 
└── frontend/
    ├── src/
    │   └── app/
    │       ├── globals.css  # Light monochromatic design system stylesheet
    │       ├── layout.tsx   # Root document wrapper
    │       └── page.tsx     # Interactive dashboard with timeline logs
    ├── .next/
    │   └── cache/           # Symlink to local APFS storage (Turbopack cache)
    ├── package.json         # Node.js dependencies
    └── next.config.ts       # Next.js configurations
```

---

## Technical Optimizations and Workarounds

### 1. Ollama Embedding Batching
When indexing large corpora, importing thousands of document chunks simultaneously can overload the Ollama embedding client, resulting in a connection failure (`ollama._types.ResponseError: Post "/tokenize": EOF`). To prevent this, the ingestion module initializes the Chroma database on disk with the first chunk, and then inserts all remaining chunks in batches of 100. This ensures stable memory consumption and reliable embedding generation.

### 2. SQLite exFAT File Locking Workaround
If the project files reside on an external SSD formatted as **exFAT** under macOS, executing database writes directly in the project folder will trigger SQLite errors (`attempt to write a readonly database`). This occurs because the exFAT filesystem does not support standard POSIX locks needed by SQLite. 
To resolve this, the actual database files are stored in the host APFS partition (under the user's home directory `/Users/udit/.gemini/antigravity/chroma_db`) and symlinked into the backend folder (`backend/chroma_db`). The sqlite journal/WAL mechanisms write successfully to the APFS location.

### 3. Next.js Turbopack Cache exFAT Workaround
Similar to SQLite, the Turbopack build engine used by Next.js uses a local database for caching builds (`.next/cache/dev/cache/turbopack`). Running `npm run dev` directly on an exFAT partition triggers `[Error: Failed to open database ... Caused by: 0: Loading persistence directory failed]`. 
To fix this, we preserve `.next` as a regular directory so that Node.js module resolution works correctly within the project's `node_modules` path, and symlink **only the cache directory** (`.next/cache`) to the APFS system disk (under the user's home directory `/Users/udit/.gemini/antigravity/next_cache`).

---

## Ingestion and In-Memory Fallback
- **ChromaDB Loading Check**: The backend verification checks if `backend/chroma_db/chroma.sqlite3` exists. If the folder is empty or not initialized, it automatically indexes the PDFs in `Docs/` on the first search request, persists the database to disk, and loads it for subsequent queries.
- **Tavily Key Check**: If `TAVILY_API_KEY` is omitted in the `.env` file, the backend skips the web search node gracefully rather than raising an unhandled exception, displaying a status warning in the frontend dashboard.

---

## Running the Application

### 1. Local Prerequisites
Ensure that the local Ollama daemon is running and has the required models downloaded:
```bash
ollama pull gemma3:4b
ollama pull embeddinggemma:latest
```

### 2. Start the Backend API
1. Navigate to the backend directory:
   ```bash
   cd backend
   ```
2. Create and activate a Python virtual environment:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```
3. Install the dependencies:
   ```bash
   pip install -r requirements.txt
   ```
4. Copy the `.env.example` file to `.env` and fill in your Tavily API Key (optional):
   ```bash
   cp .env.example .env
   ```
5. Start the FastAPI server:
   ```bash
   python3 main.py
   ```
   The API will be available at `http://localhost:8000`.

### 3. Start the Frontend Dashboard
1. Open a new terminal tab and navigate to the frontend directory:
   ```bash
   cd frontend
   ```
2. Install the Node packages:
   ```bash
   npm install
   ```
3. Start the Next.js development server:
   ```bash
   npm run dev
   ```
   The dashboard will be running at `http://localhost:3000`.
